# การใช้งานโปรแกรมตรวจจับรถยนต์ GUI

## คุณสมบัติของโปรแกรม

### 🚗 โปรแกรมตรวจจับรถยนต์ พร้อม GUI
โปรแกรมนี้มีส่วนประกอบหลัก 2 ส่วน:
1. **car_detection.py** - โปรแกรมหลักแบบ Command Line
2. **car_detection_gui.py** - โปรแกรมแบบ GUI (Graphical User Interface)

### 📱 คุณสมบัติของ GUI
- **ส่วนต่อประสานที่ใช้งานง่าย** - ออกแบบมาให้ใช้งานง่าย เหมาะสำหรับผู้ใช้ทั่วไป
- **การแสดงผลแบบ Real-time** - ดูผลลัพธ์การตรวจจับได้ทันที
- **การควบคุมการทำงาน** - เริ่ม หยุด และตั้งค่าต่างๆ ได้อย่างสะดวก
- **สถิติการทำงาน** - แสดงจำนวนเฟรม จำนวนรถที่พบ และ FPS
- **Progress Bar** - แสดงความคืบหน้าในการประมวลผล

### 🎛️ ฟีเจอร์หลัก
1. **เลือกไฟล์วิดีโอ** - รองรับไฟล์ .mp4, .avi, .mov, .mkv, .wmv, .flv
2. **ตั้งค่าความเชื่อมั่น** - ปรับระดับความแม่นยำของการตรวจจับ (0.1-0.9)
3. **บันทึกผลลัพธ์** - เลือกได้ว่าจะบันทึกวิดีโอที่มีการ annotation หรือไม่
4. **โหมด Webcam** - ตรวจจับรถจากกล้อง webcam แบบ real-time
5. **การแสดงสถิติ** - แสดงข้อมูลการทำงานแบบ real-time

## 🚀 การติดตั้งและใช้งาน

### ข้อกำหนดระบบ
```bash
pip install opencv-python ultralytics pillow
```

### การใช้งาน GUI
```bash
python car_detection_gui.py
```

### ขั้นตอนการใช้งาน
1. **เปิดโปรแกรม** - รันไฟล์ `car_detection_gui.py`
2. **เลือกไฟล์วิดีโอ** - กดปุ่ม "เลือกไฟล์วิดีโอ" และเลือกไฟล์ที่ต้องการ
3. **ตั้งค่า** - ปรับระดับความเชื่อมั่นและเลือกว่าจะบันทึกผลลัพธ์หรือไม่
4. **เริ่มประมวลผล** - กดปุ่ม "เริ่มประมวลผล" หรือ "เริ่มกล้อง Webcam"
5. **ดูผลลัพธ์** - ติดตามการทำงานผ่านหน้าจอแสดงผลและสถิติ
6. **หยุดการทำงาน** - กดปุ่ม "หยุดการทำงาน" เมื่อต้องการหยุด

## 🎨 ส่วนประกอบของ GUI

### แผงควบคุมซ้าย
- **เลือกไฟล์วิดีโอ** - เลือกไฟล์ input
- **การตั้งค่า** - ปรับความเชื่อมั่นและเลือกการบันทึก
- **ปุ่มควบคุม** - เริ่ม/หยุดการทำงาน
- **สถิติการตรวจจับ** - แสดงข้อมูลการทำงาน
- **Progress Bar** - แสดงความคืบหน้า

### แผงแสดงผลขวา
- **Canvas แสดงวิดีโอ** - แสดงวิดีโอที่มีการ annotation
- **การปรับขนาดอัตโนมัติ** - ปรับขนาดให้เหมาะกับหน้าจอ

## 🎯 ประเภทรถที่ตรวจจับได้
- 🚗 **รถยนต์** (สีเขียว)
- 🏍️ **จักรยานยนต์** (สีน้ำเงิน)
- 🚌 **รถบัส** (สีแดง)
- 🚛 **รถบรรทุก** (สีฟ้า)

## ⚙️ การตั้งค่า

### ระดับความเชื่อมั่น (Confidence Threshold)
- **0.1-0.3**: ตรวจจับได้มาก อาจมีผลลัพธ์ผิด (False Positive)
- **0.4-0.6**: สมดุลระหว่างความแม่นยำและการตรวจจับ (แนะนำ)
- **0.7-0.9**: ตรวจจับแม่นยำมาก อาจพลาดบางกรณี

### การบันทึกผลลัพธ์
- เลือก "บันทึกวิดีโอผลลัพธ์" หากต้องการเก็บไฟล์วิดีโอที่มี annotation
- โปรแกรมจะให้เลือกที่บันทึกเมื่อเริ่มประมวลผล

## 🔧 เทคนิค

### การใช้งาน Webcam
1. กดปุ่ม "เริ่มกล้อง Webcam"
2. อนุญาตการใช้งานกล้องหากระบบขออนุญาต
3. ดูผลลัพธ์การตรวจจับแบบ real-time
4. กดปุ่ม "หยุดการทำงาน" เมื่อต้องการหยุด

### การประมวลผลไฟล์วิดีโอขนาดใหญ่
- โปรแกรมรองรับการประมวลผลแบบ multi-threading
- แสดง Progress Bar เพื่อติดตามความคืบหน้า
- สามารถหยุดการทำงานได้ตลอดเวลา

## 🚨 ข้อควรระวัง
- ต้องมีไฟล์ `yolov8n.pt` หรือโปรแกรมจะดาวน์โหลดอัตโนมัติ
- การประมวลผลวิดีโอใช้ทรัพยากรระบบค่อนข้างมาก
- แนะนำให้ปิดโปรแกรมอื่นๆ เพื่อประสิทธิภาพที่ดีที่สุด

## 🎥 รูปแบบไฟล์ที่รองรับ
- MP4 (.mp4)
- AVI (.avi) 
- MOV (.mov)
- MKV (.mkv)
- WMV (.wmv)
- FLV (.flv)

---
**หมายเหตุ**: โปรแกรมใช้ YOLO v8 ซึ่งมีความแม่นยำสูงในการตรวจจับวัตถุ และรองรับการทำงานบน CPU และ GPU